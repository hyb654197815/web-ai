# 原理说明

`portable-ai-agent-widget` 的核心原理不是“让模型直接读整个前端仓库”，而是先把业务项目整理成稳定的页面知识文档，再让运行时 Agent 和编码 Agent 共用这套知识。

## 1. 为什么先生成知识文档

大型前端项目直接交给模型读取源码，常见问题是：

- 页面入口定位慢
- 路由和页面关系不稳定
- 业务语义分散在组件、接口、store、弹窗里
- 不同 AI 工具之间难以复用上下文

`webGenerate` 的做法是先把项目沉淀成两类文档：

- `routes.md`：全局路由索引
- `page-xxx.md`：单页面业务说明

这样无论是运行时问答，还是 Cursor / Codex / Claude 之类的编码助手，都能先通过业务文档定位页面，再进入源码。

## 2. 为什么 CLI 只负责安装

本项目把 `webGenerate` CLI 设计成“安装入口”而不是“源码分析器”，原因是：

- 不同助手的上下文能力不同
- 不同平台需要不同的 Skill、Hook、项目规则
- 真正的分析动作更适合在助手内完成

因此：

- `webGenerate <platform> install` 负责把工作流装进对应助手
- `/webGenerate .` 或 `$webGenerate .` 才真正触发文档生成

这样可以把一套知识库工作流复用到 Codex、Claude Code、OpenCode、Copilot、Cursor、Gemini、Trae 等不同工具里。

## 3. 运行时为什么只开放三类动作

运行时 Widget 故意把能力收窄到：

- 问答
- 导航
- 当前页表单操作

这是为了控制风险边界。

如果让后端直接下发脚本、选择器或任意 DOM 指令，会带来：

- 安全风险
- 行为不可预测
- 集成成本高
- 调试困难

现在的实现是：

1. 后端只返回结构化动作
2. 前端只接受白名单动作
3. 表单执行交给页面内的 `page-agent`
4. 模型密钥保留在服务端代理

这样既能让用户获得“帮我操作页面”的体验，又不会把浏览器执行权直接交给后端输出。

## 4. 路由问答为什么依赖当前页面上下文

Widget 会附带当前：

- `pathname`
- `hash`
- `href`
- `title`

原因是用户提问通常是上下文相关的，比如：

- “这个页面怎么新增用户”
- “下一步点哪里”
- “这里的筛选条件怎么填”

如果没有当前页面上下文，后端只能做泛化回答；带上这些信息后，后端能更准确地从 `routes.md` 和对应的 `page-xxx.md` 中选出最相关页面。

## 5. 增量同步为什么重要

项目进入日常开发后，知识文档必须能低成本维护，否则很快就会过期。

所以工作流支持：

- 全量生成：首次建库、结构性重构后重建
- 增量同步：日常开发后仅更新受影响页面

对应触发方式：

- 全量：`/webGenerate .`
- 增量：`/webGenerate . --update`

Codex 中对应为：

- 全量：`$webGenerate .`
- 增量：`$webGenerate . --update`

## 6. 为什么这个包适合发到 npm

发布到官方 npm 后，使用门槛会明显降低：

- 业务项目可以直接 `npm install`
- 不需要手动复制 CLI 脚本
- 前端 Widget 和 `webGenerate` 安装入口可以统一分发
- 文档、版本和产物能跟着 npm 版本管理

因此 README 最适合聚焦“怎么安装、怎么接入、怎么发布”，而把设计思路和体系说明拆到独立文档中。
